# pnpm 11 起，overrides 等设置从 package.json 迁到本文件。
#
# postcss：Next 钉的是 8.4.31，命中 4 条公告（2 高危 + 2 中危）。
# 可利用性评估：本项目 PostCSS 只在**构建期**处理我们自己的 CSS
# （src/ 下零引用、无运行时 CSS 处理），因此当前站点不存在可直接利用的运行时漏洞。
# 仍统一到已修补版本的理由：8.4.31 已 4 年多未更新；将来若引入运行时 CSS 处理，
# 这些公告会立刻变成活漏洞。
overrides:
  postcss: ^8.5.28
